$("#list-user").html("<%= j render partial: "user", collection: @users, as: :item %>");
$("#paginator-user").html("<%= j paginate @users, remote: true, theme: "twitter-bootstrap-4", pagination_class: "pagination-sm" %>");
$("#total-count-user").html("<%= j @users.total_count %>")
