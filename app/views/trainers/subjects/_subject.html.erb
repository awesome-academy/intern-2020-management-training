<tr id="row-<%= subject.id %>">
  <th scope="row"><%= subject_counter + sizing(@subjects) %>
  <td class="td-subject-thumbnail">
    <% if subject.image.present? %>
      <%= image_tag subject.image.url, class: "img-thumbnail thumbail" %>
    <% end %>
  </td>
  <td><%= subject.name %></td>
  <td><%= subject.duration %></td>
  <td><%= subject.note %></td>
  <td>
    <button type="button" class="btn btn-sm btn-outline-info">
      <%= icon "fas", "eye" %>
    </button>
    <button type="button" class="btn btn-sm btn-outline-primary">
      <%= icon "fas", "pencil-alt" %>
    </button>
    <%= link_to trainers_subjects_path(subject), class: "btn btn-sm btn-outline-danger delete-subject-item",
      type: "button", data: {id: "#{subject.id}"} do %>
      <%= icon "fas", "trash-alt" %>
    <% end %>
  </td>
</tr>
